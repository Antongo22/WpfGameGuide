<Page x:Class="WpfGameGuide.Chapters.Chapter7_Graphs.Answers.MazeAnswer"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Ответ: Лабиринт"
      Focusable="True" Loaded="Page_Loaded" KeyDown="Page_KeyDown">

    <Grid Background="#1A252F">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Заголовок -->
        <Border Grid.Row="0" Background="#2C3E50" Padding="15">
            <Grid>
                <StackPanel>
                    <TextBlock Text="✅ Ответ: Игра-Лабиринт" FontSize="20" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Глава 7 — Графы и поиск пути (BFS)" Foreground="#95A5A6" Margin="0,3,0,0"/>
                </StackPanel>
                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                    <TextBlock Text="Шагов: " Foreground="#95A5A6" VerticalAlignment="Center"/>
                    <TextBlock x:Name="StepsText" Text="0" Foreground="#F39C12" FontWeight="Bold" 
                               FontSize="18" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Игровое поле -->
        <Border Grid.Row="1" Background="#1a1a1a" Margin="10" CornerRadius="5">
            <Canvas x:Name="MazeCanvas" Background="#1a1a1a" ClipToBounds="True" 
                    SizeChanged="MazeCanvas_SizeChanged"/>
        </Border>
        
        <!-- Панель управления -->
        <Border Grid.Row="2" Background="#2C3E50" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button Content="🆕 Новый лабиринт" Width="130" Height="35" 
                            Background="#3498DB" Foreground="White" Margin="0,0,10,0"
                            Click="NewMaze_Click"/>
                    <Button Content="🔍 Показать путь" Width="120" Height="35" 
                            Background="#9B59B6" Foreground="White"
                            Click="ShowPath_Click"/>
                </StackPanel>
                
                <TextBlock Grid.Column="1" x:Name="StatusText" Text="Найдите выход! Используйте ↑↓←→" 
                           Foreground="#95A5A6" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                
                <TextBlock Grid.Column="2" x:Name="TimerText" Text="" 
                           Foreground="#27AE60" FontWeight="Bold" VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Page>
