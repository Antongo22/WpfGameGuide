<Page x:Class="WpfApp_Book.Chapters.Chapter3_Keyboard.Demo.KeyboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Глава 3: Клавиатура" Focusable="True" Loaded="Page_Loaded" KeyDown="Page_KeyDown">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
        <StackPanel>
            <TextBlock Text="Глава 3: Работа с клавиатурой" 
                       FontSize="28" FontWeight="Bold" Margin="0,0,0,20" Foreground="#2E86AB"/>
            
            <Border Background="#F0F4F8" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="⌨️ События клавиатуры" FontSize="18" FontWeight="Bold" Foreground="#1A5276"/>
                    <TextBlock TextWrapping="Wrap" Margin="0,10,0,0" FontSize="13" FontFamily="Consolas">
                        • KeyDown — клавиша нажата&#x0a;
                        • KeyUp — клавиша отпущена&#x0a;
                        • PreviewKeyDown — перехват ДО обработки
                    </TextBlock>
                </StackPanel>
            </Border>

            <Border Background="#E8F6F3" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="⇧ Модификаторы" FontSize="18" FontWeight="Bold" Foreground="#1A5276"/>
                    <Border Background="#2C3E50" CornerRadius="4" Padding="10" Margin="0,10,0,0">
                        <TextBlock FontFamily="Consolas" Foreground="White" FontSize="11">
                            if (e.Key == Key.S &amp;&amp; Keyboard.Modifiers == ModifierKeys.Control)&#x0a;{&#x0a;    // Ctrl+S&#x0a;}
                        </TextBlock>
                    </Border>
                </StackPanel>
            </Border>

            <Border Background="#D5F5E3" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="🎮 Демонстрация" FontSize="18" FontWeight="Bold" Foreground="#27AE60"/>
                    <TextBlock Text="Нажимайте клавиши:" Margin="0,10,0,10"/>
                    <Border Background="White" CornerRadius="8" Padding="15" BorderBrush="#27AE60" BorderThickness="2">
                        <StackPanel>
                            <TextBlock x:Name="KeyPressedText" Text="Нажатая клавиша: —" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="ModifiersText" Text="Модификаторы: —" FontSize="14" HorizontalAlignment="Center" Foreground="#666"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <Border Background="#FADBD8" CornerRadius="8" Padding="15" BorderBrush="#E74C3C" BorderThickness="2">
                <StackPanel>
                    <TextBlock Text="📋 Задание: Игра 'Нажми клавишу'" FontSize="18" FontWeight="Bold" Foreground="#C0392B"/>
                    <TextBlock TextWrapping="Wrap" Margin="0,10,0,0" FontSize="13">
                        • Показывать случайную букву A-Z&#x0a;
                        • Правильно = +1 очко, неправильно = -1&#x0a;
                        • Счёт и кнопка сброса
                    </TextBlock>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
