<Page x:Class="WpfGameGuide.Chapters.Chapter2_PagesAndWindows.Answers.NotesAnswer"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Ответ: Заметки">

    <Grid Background="#1A252F">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Заголовок -->
        <Border Grid.Row="0" Background="#2C3E50" Padding="20">
            <StackPanel>
                <TextBlock Text="✅ Ответ: Приложение-заметки" FontSize="24" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="Глава 2 — Страницы, вкладки и окна" Foreground="#95A5A6" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>
        
        <!-- Основной контент -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>
            
            <!-- Список заметок -->
            <Border Grid.Column="0" Background="#34495E" CornerRadius="10" Padding="15" Margin="0,0,10,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Text="📝 Мои заметки" FontSize="18" FontWeight="Bold" 
                               Foreground="White" Margin="0,0,0,10"/>
                    
                    <ListBox x:Name="NotesListBox" Grid.Row="1" Background="#2C3E50" 
                             BorderThickness="0" Foreground="White" FontSize="14"
                             SelectionChanged="NotesListBox_SelectionChanged"/>
                </Grid>
            </Border>
            
            <!-- Панель управления -->
            <Border Grid.Column="1" Background="#34495E" CornerRadius="10" Padding="15">
                <StackPanel>
                    <TextBlock Text="Новая заметка:" Foreground="White" FontWeight="Bold" Margin="0,0,0,5"/>
                    <TextBox x:Name="NewNoteTextBox" Height="100" TextWrapping="Wrap" 
                             AcceptsReturn="True" Padding="10" FontSize="14"/>
                    
                    <Button Content="➕ Добавить" Height="40" Margin="0,10,0,0"
                            Background="#27AE60" Foreground="White" FontWeight="Bold"
                            Click="AddNote_Click"/>
                    
                    <Separator Margin="0,20"/>
                    
                    <TextBlock Text="Выбранная заметка:" Foreground="White" FontWeight="Bold" Margin="0,0,0,5"/>
                    <TextBox x:Name="SelectedNoteTextBox" Height="100" TextWrapping="Wrap" 
                             AcceptsReturn="True" Padding="10" FontSize="14" IsReadOnly="True"/>
                    
                    <Button Content="✏️ Редактировать" Height="35" Margin="0,10,0,0"
                            Background="#3498DB" Foreground="White"
                            Click="EditNote_Click"/>
                    
                    <Button Content="🗑️ Удалить" Height="35" Margin="0,10,0,0"
                            Background="#E74C3C" Foreground="White"
                            Click="DeleteNote_Click"/>
                    
                    <Button Content="🧹 Очистить всё" Height="35" Margin="0,10,0,0"
                            Background="#95A5A6" Foreground="White"
                            Click="ClearAll_Click"/>
                    
                    <TextBlock x:Name="CountText" Text="Заметок: 0" Foreground="#95A5A6" 
                               Margin="0,15,0,0" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
