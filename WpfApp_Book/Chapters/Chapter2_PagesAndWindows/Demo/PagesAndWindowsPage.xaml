<Page x:Class="WpfGameGuide.Chapters.Chapter2_PagesAndWindows.Demo.PagesAndWindowsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Глава 2: Страницы и окна">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock Text="Глава 2: Страницы, вкладки и окна" 
                       FontSize="28" FontWeight="Bold" Margin="0,0,0,20"
                       Foreground="#2E86AB"/>
            
            <!-- ===== ТЕОРИЯ: Window vs Page ===== -->
            <Border Background="#F0F4F8" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="🪟 Window vs Page" FontSize="18" FontWeight="Bold" Foreground="#1A5276"/>
                    <TextBlock TextWrapping="Wrap" Margin="0,10,0,10" FontSize="13">
В WPF есть два основных контейнера для контента: Window и Page.
                    </TextBlock>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <Border Grid.Column="0" Background="#D4E6F1" CornerRadius="5" Padding="10" Margin="0,0,5,0">
                            <StackPanel>
                                <TextBlock Text="Window (Окно)" FontWeight="Bold" FontSize="14"/>
                                <StackPanel Margin="0,5,0,0">
                                    <TextBlock Text="• Отдельное окно с заголовком" FontSize="12"/>
                                    <TextBlock Text="• Может быть свёрнуто/развёрнуто" FontSize="12"/>
                                    <TextBlock Text="• Имеет свою панель задач" FontSize="12"/>
                                    <TextBlock Text="• Для главного окна и диалогов" FontSize="12"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                        
                        <Border Grid.Column="1" Background="#D5F5E3" CornerRadius="5" Padding="10" Margin="5,0,0,0">
                            <StackPanel>
                                <TextBlock Text="Page (Страница)" FontWeight="Bold" FontSize="14"/>
                                <StackPanel Margin="0,5,0,0">
                                    <TextBlock Text="• Отображается внутри Frame" FontSize="12"/>
                                    <TextBlock Text="• Нет собственного окна" FontSize="12"/>
                                    <TextBlock Text="• Подходит для навигации" FontSize="12"/>
                                    <TextBlock Text="• Легче переключать контент" FontSize="12"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </Grid>
                    
                    <Border Background="#2C3E50" CornerRadius="4" Padding="10" Margin="0,10,0,0">
                        <TextBlock FontFamily="Consolas" Foreground="White" xml:space="preserve" FontSize="12">
&lt;!-- MainWindow.xaml: Frame для отображения страниц --&gt;
&lt;Frame x:Name="MainFrame" NavigationUIVisibility="Hidden"/&gt;

// Переход на страницу:
MainFrame.Navigate(new MyPage());

// Или через Source:
&lt;Frame Source="Pages/HomePage.xaml"/&gt;
                        </TextBlock>
                    </Border>
                </StackPanel>
            </Border>

            <!-- ===== ТЕОРИЯ: TabControl ===== -->
            <Border Background="#E8F6F3" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="📑 TabControl (Вкладки)" FontSize="18" FontWeight="Bold" Foreground="#1E8449"/>
                    <TextBlock TextWrapping="Wrap" Margin="0,10,0,10" FontSize="13">
TabControl — элемент для организации контента по вкладкам. Каждая вкладка — TabItem.
                    </TextBlock>
                    
                    <Border Background="#2C3E50" CornerRadius="4" Padding="10" Margin="0,0,0,10">
                        <TextBlock FontFamily="Consolas" Foreground="White" xml:space="preserve" FontSize="12">
&lt;TabControl&gt;
    &lt;TabItem Header="Вкладка 1"&gt;
        &lt;TextBlock Text="Контент первой вкладки"/&gt;
    &lt;/TabItem&gt;
    &lt;TabItem Header="Вкладка 2"&gt;
        &lt;Frame Source="Pages/Page2.xaml"/&gt;
    &lt;/TabItem&gt;
&lt;/TabControl&gt;

// Программное переключение:
myTabControl.SelectedIndex = 1;  // Вторая вкладка
                        </TextBlock>
                    </Border>
                    
                    <!-- Демонстрация TabControl -->
                    <TextBlock Text="Демонстрация:" FontWeight="SemiBold" Margin="0,5,0,5"/>
                    <TabControl Height="100">
                        <TabItem Header="Главная">
                            <TextBlock Text="Это контент главной вкладки" Margin="10"/>
                        </TabItem>
                        <TabItem Header="Настройки">
                            <StackPanel Margin="10">
                                <CheckBox Content="Включить уведомления" IsChecked="True"/>
                                <CheckBox Content="Тёмная тема"/>
                            </StackPanel>
                        </TabItem>
                        <TabItem Header="О программе">
                            <TextBlock Text="WPF Учебник v1.0" Margin="10" FontStyle="Italic"/>
                        </TabItem>
                    </TabControl>
                </StackPanel>
            </Border>

            <!-- ===== ТЕОРИЯ: Открытие окон ===== -->
            <Border Background="#FEF9E7" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="🚪 Открытие окон: Show() vs ShowDialog()" FontSize="18" FontWeight="Bold" Foreground="#B7950B"/>
                    <TextBlock TextWrapping="Wrap" Margin="0,10,0,10" FontSize="13">
Есть два способа открыть окно: обычное (Show) и модальное (ShowDialog).
                    </TextBlock>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <Border Grid.Column="0" Background="White" CornerRadius="5" Padding="10" Margin="0,0,5,0">
                            <StackPanel>
                                <TextBlock Text="Show() — обычное" FontWeight="Bold" FontSize="13"/>
                                <StackPanel Margin="0,5,0,0">
                                    <TextBlock Text="• Не блокирует родительское окно" FontSize="12"/>
                                    <TextBlock Text="• Можно работать с обоими" FontSize="12"/>
                                    <TextBlock Text="• Для вспомогательных панелей" FontSize="12"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                        
                        <Border Grid.Column="1" Background="White" CornerRadius="5" Padding="10" Margin="5,0,0,0">
                            <StackPanel>
                                <TextBlock Text="ShowDialog() — модальное" FontWeight="Bold" FontSize="13"/>
                                <StackPanel Margin="0,5,0,0">
                                    <TextBlock Text="• Блокирует родительское окно" FontSize="12"/>
                                    <TextBlock Text="• Нужно закрыть для продолжения" FontSize="12"/>
                                    <TextBlock Text="• Для диалогов и подтверждений" FontSize="12"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </Grid>
                    
                    <Border Background="#2C3E50" CornerRadius="4" Padding="10" Margin="0,10,0,10">
                        <TextBlock FontFamily="Consolas" Foreground="White" xml:space="preserve" FontSize="12">
// Обычное окно
var window = new SettingsWindow();
window.Show();  // Код продолжает выполняться сразу

// Модальное окно (диалог)
var dialog = new ConfirmDialog();
bool? result = dialog.ShowDialog();  // Код ждёт закрытия окна

if (result == true)
{
    // Пользователь нажал OK
}

// В диалоговом окне:
this.DialogResult = true;   // Закрыть с результатом "да"
this.DialogResult = false;  // Закрыть с результатом "нет"
this.Close();               // Просто закрыть (результат = null)
                        </TextBlock>
                    </Border>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                        <Button Content="Открыть окно (Show)" Padding="10,5" Margin="0,0,10,0" Click="OpenWindow_Click"/>
                        <Button Content="Модальное (ShowDialog)" Padding="10,5" Click="OpenDialog_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- ===== ТЕОРИЯ: MessageBox ===== -->
            <Border Background="#F5EEF8" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="💬 MessageBox — быстрые диалоги" FontSize="18" FontWeight="Bold" Foreground="#8E44AD"/>
                    <TextBlock TextWrapping="Wrap" Margin="0,10,0,10" FontSize="13">
MessageBox — встроенный класс для простых диалогов без создания отдельного окна.
                    </TextBlock>
                    
                    <Border Background="#2C3E50" CornerRadius="4" Padding="10" Margin="0,0,0,10">
                        <TextBlock FontFamily="Consolas" Foreground="White" xml:space="preserve" FontSize="12">
// Простое сообщение
MessageBox.Show("Операция завершена!");

// С заголовком
MessageBox.Show("Файл сохранён", "Успех");

// С кнопками и иконкой
MessageBoxResult result = MessageBox.Show(
    "Вы уверены, что хотите удалить?",     // Текст
    "Подтверждение удаления",              // Заголовок
    MessageBoxButton.YesNoCancel,          // Кнопки
    MessageBoxImage.Warning                // Иконка
);

if (result == MessageBoxResult.Yes)
{
    // Пользователь нажал "Да"
}
                        </TextBlock>
                    </Border>
                    
                    <TextBlock Text="Типы кнопок:" FontWeight="SemiBold" Margin="0,5,0,5"/>
                    <Border Background="White" CornerRadius="4" Padding="10" Margin="0,0,0,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="OK" FontWeight="Bold" Margin="0,0,15,3" FontFamily="Consolas"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="Только кнопка OK"/>
                            
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="OKCancel" FontWeight="Bold" Margin="0,0,15,3" FontFamily="Consolas"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="OK и Отмена"/>
                            
                            <TextBlock Grid.Row="2" Grid.Column="0" Text="YesNo" FontWeight="Bold" Margin="0,0,15,3" FontFamily="Consolas"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="Да и Нет"/>
                            
                            <TextBlock Grid.Row="3" Grid.Column="0" Text="YesNoCancel" FontWeight="Bold" Margin="0,0,15,0" FontFamily="Consolas"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" Text="Да, Нет и Отмена"/>
                        </Grid>
                    </Border>
                    
                    <StackPanel Orientation="Horizontal">
                        <Button Content="Простое сообщение" Padding="10,5" Margin="0,0,10,0" Click="ShowMessage_Click"/>
                        <Button Content="Вопрос Да/Нет" Padding="10,5" Click="ShowYesNo_Click"/>
                    </StackPanel>
                    <TextBlock x:Name="MessageBoxResult" Text="" Margin="0,10,0,0" FontWeight="Bold" Foreground="#8E44AD"/>
                </StackPanel>
            </Border>

            <!-- ===== ТЕОРИЯ: Передача данных между окнами ===== -->
            <Border Background="#EAEDED" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="📤 Передача данных между окнами" FontSize="18" FontWeight="Bold" Foreground="#566573"/>
                    <TextBlock TextWrapping="Wrap" Margin="0,10,0,10" FontSize="13">
Часто нужно передать данные в открываемое окно или получить результат из диалога:
                    </TextBlock>
                    
                    <Border Background="#2C3E50" CornerRadius="4" Padding="10">
                        <TextBlock FontFamily="Consolas" Foreground="White" xml:space="preserve" FontSize="11">
// Способ 1: Через конструктор
public class EditDialog : Window
{
    public string EditedText { get; set; }
    
    public EditDialog(string initialText)
    {
        InitializeComponent();
        MyTextBox.Text = initialText;
    }
}

// Использование:
var dialog = new EditDialog("Начальный текст");
if (dialog.ShowDialog() == true)
{
    string result = dialog.EditedText;
}

// Способ 2: Через публичные свойства
var dialog = new EditDialog();
dialog.InitialValue = "Начальный текст";
dialog.ShowDialog();
string result = dialog.ResultValue;
                        </TextBlock>
                    </Border>
                </StackPanel>
            </Border>

            <!-- ===== ТЕОРИЯ: ListBox ===== -->
            <Border Background="#D5F5E3" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="📋 ListBox — список элементов" FontSize="18" FontWeight="Bold" Foreground="#27AE60"/>
                    <TextBlock TextWrapping="Wrap" Margin="0,10,0,10" FontSize="13">
ListBox — элемент для отображения списка элементов с возможностью выбора одного или нескольких элементов.
                    </TextBlock>
                    
                    <Border Background="#2C3E50" CornerRadius="4" Padding="10" Margin="0,0,0,10">
                        <TextBlock FontFamily="Consolas" Foreground="White" xml:space="preserve" FontSize="12">
&lt;!-- XAML: создание ListBox --&gt;
&lt;ListBox x:Name="MyListBox" 
         Height="150" 
         SelectionChanged="MyListBox_SelectionChanged"&gt;
    &lt;ListBoxItem Content="Элемент 1"/&gt;
    &lt;ListBoxItem Content="Элемент 2"/&gt;
    &lt;ListBoxItem Content="Элемент 3"/&gt;
&lt;/ListBox&gt;

// C#: работа с ListBox
// Добавление элемента
MyListBox.Items.Add("Новый элемент");

// Получение выбранного элемента
object selected = MyListBox.SelectedItem;
string selectedText = MyListBox.SelectedItem?.ToString();

// Удаление выбранного элемента
if (MyListBox.SelectedItem != null)
{
    MyListBox.Items.Remove(MyListBox.SelectedItem);
}

// Очистка всего списка
MyListBox.Items.Clear();

// Обработчик выбора
private void MyListBox_SelectionChanged(object sender, SelectionChangedEventArgs e)
{
    if (MyListBox.SelectedItem != null)
    {
        MessageBox.Show($"Выбран: {MyListBox.SelectedItem}");
    }
}
                        </TextBlock>
                    </Border>
                    
                    <TextBlock Text="Основные свойства:" FontWeight="SemiBold" Margin="0,5,0,5"/>
                    <Border Background="White" CornerRadius="4" Padding="10" Margin="0,0,0,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Items" FontWeight="Bold" Margin="0,0,15,3" FontFamily="Consolas"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="Коллекция элементов списка"/>
                            
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="SelectedItem" FontWeight="Bold" Margin="0,0,15,3" FontFamily="Consolas"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="Выбранный элемент (или null)"/>
                            
                            <TextBlock Grid.Row="2" Grid.Column="0" Text="SelectedIndex" FontWeight="Bold" Margin="0,0,15,3" FontFamily="Consolas"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="Индекс выбранного элемента (-1 если ничего не выбрано)"/>
                            
                            <TextBlock Grid.Row="3" Grid.Column="0" Text="SelectionMode" FontWeight="Bold" Margin="0,0,15,0" FontFamily="Consolas"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" Text="Single (один) или Multiple (несколько)"/>
                        </Grid>
                    </Border>
                    
                    <!-- Демонстрация ListBox -->
                    <TextBlock Text="Демонстрация:" FontWeight="SemiBold" Margin="0,5,0,5"/>
                    <Border Background="White" CornerRadius="8" Padding="15" BorderBrush="#27AE60" BorderThickness="2">
                        <StackPanel>
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="ListBoxInput" Grid.Column="0" Margin="0,0,10,0" 
                                         Text="Новая заметка" VerticalContentAlignment="Center"/>
                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <Button Content="Добавить" Padding="10,5" Margin="0,0,5,0" 
                                            Click="AddToListBox_Click" Background="#27AE60" Foreground="White"/>
                                    <Button Content="Удалить" Padding="10,5" 
                                            Click="RemoveFromListBox_Click" Background="#E74C3C" Foreground="White"/>
                                </StackPanel>
                            </Grid>
                            <ListBox x:Name="DemoListBox" Height="120" SelectionChanged="DemoListBox_SelectionChanged">
                                <ListBoxItem Content="Заметка 1"/>
                                <ListBoxItem Content="Заметка 2"/>
                                <ListBoxItem Content="Заметка 3"/>
                            </ListBox>
                            <TextBlock x:Name="ListBoxStatus" Text="Выберите элемент из списка" 
                                       Margin="0,10,0,0" FontStyle="Italic" Foreground="#666"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <!-- ===== ПРАКТИЧЕСКОЕ ЗАДАНИЕ ===== -->
            <Border Background="#FADBD8" CornerRadius="8" Padding="15" BorderBrush="#E74C3C" BorderThickness="2">
                <StackPanel>
                    <TextBlock Text="📋 Практическое задание: Приложение-заметки" 
                               FontSize="18" FontWeight="Bold" Foreground="#C0392B"/>
                    <TextBlock TextWrapping="Wrap" Margin="0,10,0,10" FontSize="13">
Создайте приложение для управления заметками с подтверждением удаления.
                    </TextBlock>
                    
                    <Border Background="White" CornerRadius="5" Padding="15">
                        <StackPanel>
                            <TextBlock Text="Требования:" FontWeight="Bold" Margin="0,0,0,10"/>
                            
                            <StackPanel Margin="0,0,0,5">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="✅" Margin="0,0,8,0"/>
                                    <TextBlock Text="ListBox для отображения списка заметок"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <StackPanel Margin="0,0,0,5">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="✅" Margin="0,0,8,0"/>
                                    <TextBlock Text="TextBox для ввода новой заметки"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <StackPanel Margin="0,0,0,5">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="✅" Margin="0,0,8,0"/>
                                    <TextBlock Text="Кнопка 'Добавить' для добавления заметки"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <StackPanel Margin="0,0,0,5">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="✅" Margin="0,0,8,0"/>
                                    <TextBlock Text="Кнопка 'Удалить' с подтверждением через MessageBox"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <StackPanel Margin="0,0,0,5">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="✅" Margin="0,0,8,0"/>
                                    <TextBlock Text="Кнопка 'Очистить всё' с предупреждением"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <TextBlock Text="Бонус:" FontWeight="Bold" Margin="0,10,0,5" Foreground="#27AE60"/>
                            
                            <StackPanel Margin="0,0,0,5">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🌟" Margin="0,0,8,0"/>
                                    <TextBlock Text="Редактирование заметки в отдельном диалоговом окне"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <StackPanel Margin="0,0,0,5">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🌟" Margin="0,0,8,0"/>
                                    <TextBlock Text="Поиск/фильтрация заметок"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🌟" Margin="0,0,8,0"/>
                                    <TextBlock Text="Отображение даты создания заметки"/>
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
