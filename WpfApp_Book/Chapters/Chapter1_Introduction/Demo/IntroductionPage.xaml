<Page x:Class="WpfGameGuide.Chapters.Chapter1_Introduction.Demo.IntroductionPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Глава 1: Введение">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
        <StackPanel Margin="20">
            <!-- ===== ЗАГОЛОВОК ===== -->
            <TextBlock Text="Глава 1: Введение. Знакомство с компонентами" 
                       FontSize="28" FontWeight="Bold" Margin="0,0,0,20"
                       Foreground="#2E86AB"/>
            
            <!-- ===== ТЕОРИЯ: BUTTON ===== -->
            <Border Background="#F0F4F8" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="🔘 Button (Кнопка)" FontSize="18" FontWeight="Bold" Foreground="#1A5276"/>
                    <TextBlock TextWrapping="Wrap" Margin="0,10,0,10" FontSize="13">
Button — элемент управления, который реагирует на нажатие. Основной способ взаимодействия пользователя с приложением.
                    </TextBlock>
                    
                    <TextBlock Text="Основные свойства:" FontWeight="SemiBold" Margin="0,5,0,5"/>
                    <Border Background="#2C3E50" CornerRadius="4" Padding="10" Margin="0,0,0,10">
                        <TextBlock FontFamily="Consolas" Foreground="White" xml:space="preserve" FontSize="12">
&lt;Button Content="Нажми меня"      // Текст кнопки
        Width="100" Height="30"   // Размеры
        Margin="5,10,5,10"        // Отступы (лево, верх, право, низ)
        Padding="10,5"            // Внутренние отступы
        IsEnabled="True"          // Активна ли кнопка
        Click="Button_Click"/&gt;    // Обработчик нажатия
                        </TextBlock>
                    </Border>
                    
                    <!-- Демонстрация Button -->
                    <TextBlock Text="Демонстрация:" FontWeight="SemiBold" Margin="0,5,0,5"/>
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                        <Button Content="Обычная" Margin="0,0,10,0" Padding="15,8"/>
                        <Button Content="Неактивная" IsEnabled="False" Margin="0,0,10,0" Padding="15,8"/>
                        <Button Padding="15,8" Background="#3498DB" Foreground="White">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⭐" Margin="0,0,5,0"/>
                                <TextBlock Text="С иконкой"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- ===== ТЕОРИЯ: TEXTBLOCK vs LABEL ===== -->
            <Border Background="#E8F6F3" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="📝 TextBlock и Label" FontSize="18" FontWeight="Bold" Foreground="#1E8449"/>
                    <TextBlock TextWrapping="Wrap" Margin="0,10,0,10" FontSize="13">
TextBlock — для отображения текста. Лёгкий и быстрый.
Label — обёртка с дополнительными возможностями (горячие клавиши, Target).
                    </TextBlock>
                    
                    <Border Background="#2C3E50" CornerRadius="4" Padding="10" Margin="0,0,0,10">
                        <TextBlock FontFamily="Consolas" Foreground="White" xml:space="preserve" FontSize="12">
&lt;TextBlock Text="Простой текст"
           FontSize="14"              // Размер шрифта
           FontWeight="Bold"          // Жирность
           Foreground="DarkBlue"      // Цвет текста
           TextWrapping="Wrap"/&gt;      // Перенос строк

&lt;Label Content="С подчёркиванием: _Имя"   // _ = горячая клавиша
       Target="{Binding ElementName=NameTextBox}"/&gt;
                        </TextBlock>
                    </Border>
                    
                    <!-- Демонстрация -->
                    <StackPanel Margin="0,5,0,0">
                        <TextBlock Text="Обычный TextBlock" FontSize="14"/>
                        <TextBlock Text="Жирный текст" FontWeight="Bold" FontSize="14"/>
                        <TextBlock Text="Курсив синий" FontStyle="Italic" Foreground="DarkBlue" FontSize="14"/>
                        <TextBlock Text="Это длинный текст с переносом (TextWrapping=Wrap)" 
                                   TextWrapping="Wrap" FontSize="13" Width="250" HorizontalAlignment="Left" 
                                   Background="#D5F5E3" Padding="5"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- ===== ТЕОРИЯ: TEXTBOX ===== -->
            <Border Background="#FEF9E7" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="⌨️ TextBox (Поле ввода)" FontSize="18" FontWeight="Bold" Foreground="#B7950B"/>
                    <TextBlock TextWrapping="Wrap" Margin="0,10,0,10" FontSize="13">
TextBox — поле для ввода текста пользователем. Можно читать и устанавливать значение через свойство Text.
                    </TextBlock>
                    
                    <Border Background="#2C3E50" CornerRadius="4" Padding="10" Margin="0,0,0,10">
                        <TextBlock FontFamily="Consolas" Foreground="White" xml:space="preserve" FontSize="12">
&lt;TextBox x:Name="MyTextBox"
         Text="Начальный текст"    // Текст в поле
         Width="200"
         MaxLength="50"            // Максимум символов
         IsReadOnly="False"        // Только для чтения?
         TextChanged="TextBox_TextChanged"/&gt;  // Событие изменения

// В C#:
string value = MyTextBox.Text;     // Получить текст
MyTextBox.Text = "Новый текст";    // Установить текст
                        </TextBlock>
                    </Border>
                    
                    <!-- Демонстрация TextBox -->
                    <TextBlock Text="Попробуйте ввести текст:" FontWeight="SemiBold" Margin="0,5,0,5"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBox x:Name="DemoTextBox" Width="250" Padding="5" 
                                 TextChanged="DemoTextBox_TextChanged"/>
                        <TextBlock x:Name="TextBoxPreview" Text=" → " VerticalAlignment="Center" Margin="10,0,0,0"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- ===== ТЕОРИЯ: CHECKBOX ===== -->
            <Border Background="#F5EEF8" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="☑️ CheckBox (Флажок)" FontSize="18" FontWeight="Bold" Foreground="#8E44AD"/>
                    <TextBlock TextWrapping="Wrap" Margin="0,10,0,10" FontSize="13">
CheckBox — элемент с двумя или тремя состояниями (включено/выключено/неопределено).
Используется для настроек и выбора опций.
                    </TextBlock>
                    
                    <Border Background="#2C3E50" CornerRadius="4" Padding="10" Margin="0,0,0,10">
                        <TextBlock FontFamily="Consolas" Foreground="White" xml:space="preserve" FontSize="12">
&lt;CheckBox x:Name="MyCheckBox"
          Content="Согласен с условиями"  // Текст рядом с флажком
          IsChecked="True"                // True/False/null (для IsThreeState)
          IsThreeState="False"            // Три состояния?
          Checked="CheckBox_Checked"      // Событие: включили
          Unchecked="CheckBox_Unchecked"  // Событие: выключили
          Click="CheckBox_Click"/&gt;        // Любое изменение

// В C#:
bool? isChecked = MyCheckBox.IsChecked;  // Получить состояние
if (MyCheckBox.IsChecked == true) { ... }
                        </TextBlock>
                    </Border>
                    
                    <!-- Демонстрация CheckBox -->
                    <TextBlock Text="Демонстрация:" FontWeight="SemiBold" Margin="0,5,0,5"/>
                    <StackPanel Margin="0,5,0,0">
                        <CheckBox x:Name="CheckBox1" Content="Опция 1 (включена)" IsChecked="True" Margin="0,3"/>
                        <CheckBox x:Name="CheckBox2" Content="Опция 2 (выключена)" IsChecked="False" Margin="0,3"/>
                        <CheckBox x:Name="CheckBox3" Content="Опция 3 (три состояния)" IsThreeState="True" IsChecked="{x:Null}" Margin="0,3"/>
                        <CheckBox x:Name="CheckBoxDemo" Content="Нажми меня!" Margin="0,3" 
                                  Click="CheckBoxDemo_Click"/>
                        <TextBlock x:Name="CheckBoxStatus" Text="Статус: —" Margin="20,5,0,0" Foreground="#8E44AD"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- ===== ТЕОРИЯ: RADIOBUTTON ===== -->
            <Border Background="#D6EAF8" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="🔘 RadioButton (Переключатель)" FontSize="18" FontWeight="Bold" Foreground="#2874A6"/>
                    <TextBlock TextWrapping="Wrap" Margin="0,10,0,10" FontSize="13">
RadioButton — позволяет выбрать только ОДИН вариант из группы.
Все RadioButton с одинаковым GroupName объединяются в группу.
                    </TextBlock>
                    
                    <Border Background="#2C3E50" CornerRadius="4" Padding="10" Margin="0,0,0,10">
                        <TextBlock FontFamily="Consolas" Foreground="White" xml:space="preserve" FontSize="12">
&lt;!-- Группа 1: Размер --&gt;
&lt;RadioButton Content="Маленький" GroupName="Size" IsChecked="True"/&gt;
&lt;RadioButton Content="Средний" GroupName="Size"/&gt;
&lt;RadioButton Content="Большой" GroupName="Size"/&gt;

&lt;!-- Группа 2: Цвет (другая группа!) --&gt;
&lt;RadioButton Content="Красный" GroupName="Color"/&gt;
&lt;RadioButton Content="Синий" GroupName="Color" IsChecked="True"/&gt;

// В C#:
if (RadioSmall.IsChecked == true) { size = "small"; }
                        </TextBlock>
                    </Border>
                    
                    <!-- Демонстрация RadioButton -->
                    <TextBlock Text="Демонстрация (выберите по одному из каждой группы):" FontWeight="SemiBold" Margin="0,5,0,5"/>
                    <Grid Margin="0,5,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <Border Grid.Column="0" Background="White" CornerRadius="5" Padding="10" Margin="0,0,5,0">
                            <StackPanel>
                                <TextBlock Text="Размер:" FontWeight="Bold" Margin="0,0,0,5"/>
                                <RadioButton x:Name="RadioSmall" Content="Маленький" GroupName="SizeDemo" IsChecked="True" Margin="0,2" Click="RadioButton_Click"/>
                                <RadioButton x:Name="RadioMedium" Content="Средний" GroupName="SizeDemo" Margin="0,2" Click="RadioButton_Click"/>
                                <RadioButton x:Name="RadioLarge" Content="Большой" GroupName="SizeDemo" Margin="0,2" Click="RadioButton_Click"/>
                            </StackPanel>
                        </Border>
                        
                        <Border Grid.Column="1" Background="White" CornerRadius="5" Padding="10" Margin="5,0,0,0">
                            <StackPanel>
                                <TextBlock Text="Цвет:" FontWeight="Bold" Margin="0,0,0,5"/>
                                <RadioButton x:Name="RadioRed" Content="Красный" GroupName="ColorDemo" Margin="0,2" Click="RadioButton_Click"/>
                                <RadioButton x:Name="RadioGreen" Content="Зелёный" GroupName="ColorDemo" IsChecked="True" Margin="0,2" Click="RadioButton_Click"/>
                                <RadioButton x:Name="RadioBlue" Content="Синий" GroupName="ColorDemo" Margin="0,2" Click="RadioButton_Click"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                    <TextBlock x:Name="RadioStatus" Text="Выбрано: Маленький, Зелёный" Margin="0,10,0,0" FontWeight="Bold" Foreground="#2874A6"/>
                </StackPanel>
            </Border>

            <!-- ===== ТЕОРИЯ: СОБЫТИЯ ===== -->
            <Border Background="#EAEDED" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="⚡ События (Events)" FontSize="18" FontWeight="Bold" Foreground="#566573"/>
                    <TextBlock TextWrapping="Wrap" Margin="0,10,0,10" FontSize="13">
События — способ реагировать на действия пользователя. В XAML указываем имя события и метод-обработчик, в C# пишем код.
                    </TextBlock>
                    
                    <Border Background="#2C3E50" CornerRadius="4" Padding="10" Margin="0,0,0,10">
                        <TextBlock FontFamily="Consolas" Foreground="White" xml:space="preserve" FontSize="12">
&lt;!-- XAML: привязываем событие к методу --&gt;
&lt;Button Content="Нажми" Click="Button_Click"/&gt;
&lt;TextBox TextChanged="TextBox_TextChanged"/&gt;
&lt;CheckBox Checked="CheckBox_Checked" Unchecked="CheckBox_Unchecked"/&gt;

// C# (файл .xaml.cs): реализуем метод
private void Button_Click(object sender, RoutedEventArgs e)
{
    // sender — элемент, вызвавший событие
    Button btn = sender as Button;
    MessageBox.Show($"Нажата кнопка: {btn.Content}");
}
                        </TextBlock>
                    </Border>
                    
                    <TextBlock Text="Основные события:" FontWeight="SemiBold" Margin="0,5,0,5"/>
                    <Border Background="White" CornerRadius="4" Padding="10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Click" FontWeight="Bold" Margin="0,0,15,3"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="Нажатие (Button, CheckBox, RadioButton)"/>
                            
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="TextChanged" FontWeight="Bold" Margin="0,0,15,3"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="Изменение текста (TextBox)"/>
                            
                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Checked/Unchecked" FontWeight="Bold" Margin="0,0,15,3"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="Включение/выключение (CheckBox, RadioButton)"/>
                            
                            <TextBlock Grid.Row="3" Grid.Column="0" Text="SelectionChanged" FontWeight="Bold" Margin="0,0,15,0"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" Text="Изменение выбора (ListBox, ComboBox)"/>
                        </Grid>
                    </Border>
                </StackPanel>
            </Border>

            <!-- ===== ПРИМЕР: КЛИКЕР ===== -->
            <Border Background="#FCF3CF" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="🎮 Пример: Кликер" FontSize="18" FontWeight="Bold" Foreground="#9A7D0A"/>
                    <TextBlock TextWrapping="Wrap" Margin="0,10,0,10" FontSize="13">
Простой кликер демонстрирует работу с событием Click и обновление UI:
                    </TextBlock>
                    
                    <StackPanel HorizontalAlignment="Center" Margin="0,10,0,0">
                        <TextBlock x:Name="ClickCountText" Text="Кликов: 0" 
                                   FontSize="28" FontWeight="Bold" HorizontalAlignment="Center"
                                   Foreground="#2E86AB"/>
                        <Button x:Name="ClickerButton" Content="🖱️ КЛИК!" 
                                Width="150" Height="60" FontSize="20" FontWeight="Bold"
                                Margin="0,10,0,0" Click="ClickerButton_Click"
                                Background="#3498DB" Foreground="White"/>
                        <Button Content="↻ Сбросить" Width="100" Margin="0,10,0,0" 
                                Click="ResetButton_Click"/>
                    </StackPanel>
                    
                    <Border Background="#2C3E50" CornerRadius="4" Padding="10" Margin="0,15,0,0">
                        <TextBlock FontFamily="Consolas" Foreground="White" xml:space="preserve" FontSize="11">
private int clickCount = 0;

private void ClickerButton_Click(object sender, RoutedEventArgs e)
{
    clickCount++;
    ClickCountText.Text = $"Кликов: {clickCount}";
}
                        </TextBlock>
                    </Border>
                </StackPanel>
            </Border>

            <!-- ===== ПРАКТИЧЕСКОЕ ЗАДАНИЕ ===== -->
            <Border Background="#FADBD8" CornerRadius="8" Padding="15" Margin="0,0,0,15" 
                    BorderBrush="#E74C3C" BorderThickness="2">
                <StackPanel>
                    <TextBlock Text="📋 Практическое задание: Калькулятор" 
                               FontSize="18" FontWeight="Bold" Foreground="#C0392B"/>
                    <TextBlock TextWrapping="Wrap" Margin="0,10,0,10" FontSize="13">
Создайте простой калькулятор с операциями сложения, вычитания, умножения и деления.
                    </TextBlock>
                    
                    <Border Background="White" CornerRadius="5" Padding="15">
                        <StackPanel>
                            <TextBlock Text="Требования:" FontWeight="Bold" Margin="0,0,0,10"/>
                            
                            <StackPanel Margin="0,0,0,5">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="✅" Margin="0,0,8,0"/>
                                    <TextBlock Text="Два поля TextBox для ввода чисел"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <StackPanel Margin="0,0,0,5">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="✅" Margin="0,0,8,0"/>
                                    <TextBlock Text="Четыре кнопки для операций: +, −, ×, ÷"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <StackPanel Margin="0,0,0,5">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="✅" Margin="0,0,8,0"/>
                                    <TextBlock Text="TextBlock для отображения результата"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <StackPanel Margin="0,0,0,5">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="✅" Margin="0,0,8,0"/>
                                    <TextBlock Text="Обработка деления на ноль (показать ошибку)"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <StackPanel Margin="0,0,0,5">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="✅" Margin="0,0,8,0"/>
                                    <TextBlock Text="Проверка корректности ввода (только числа)"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <TextBlock Text="Бонус:" FontWeight="Bold" Margin="0,10,0,5" Foreground="#27AE60"/>
                            
                            <StackPanel Margin="0,0,0,5">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🌟" Margin="0,0,8,0"/>
                                    <TextBlock Text="Кнопка очистки (C)"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <StackPanel Margin="0,0,0,5">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🌟" Margin="0,0,8,0"/>
                                    <TextBlock Text="Поддержка дробных чисел (double)"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🌟" Margin="0,0,8,0"/>
                                    <TextBlock Text="История вычислений"/>
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    
                    <Border Background="#FEF9E7" CornerRadius="4" Padding="10" Margin="0,10,0,0">
                        <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="#7D6608">
💡 Подсказка: используйте double.TryParse(text, out double number) для безопасного преобразования текста в число.
                        </TextBlock>
                    </Border>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
