<Page x:Class="WpfApp_Book.Chapters.Chapter5_Animation.Demo.AnimationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Глава 5: Анимация" Loaded="Page_Loaded" Unloaded="Page_Unloaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Margin="20,20,20,10">
            <TextBlock Text="Глава 5: Анимация объектов" FontSize="28" FontWeight="Bold" Foreground="#2E86AB"/>
            
            <Border Background="#F0F4F8" CornerRadius="8" Padding="15" Margin="0,15,0,0">
                <StackPanel>
                    <TextBlock Text="⏱️ DispatcherTimer" FontSize="16" FontWeight="Bold"/>
                    <Border Background="#2C3E50" CornerRadius="4" Padding="10" Margin="0,10,0,0">
                        <TextBlock FontFamily="Consolas" Foreground="White" FontSize="11">
                            timer.Interval = TimeSpan.FromMilliseconds(16); // 60 FPS&#x0a;timer.Tick += GameLoop;&#x0a;timer.Start();
                        </TextBlock>
                    </Border>
                </StackPanel>
            </Border>
        </StackPanel>
        
        <Border Grid.Row="1" Background="#2C3E50" Margin="20,0,20,20" CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" Text="🎮 DVD-заставка" FontSize="16" FontWeight="Bold" Foreground="White" Margin="10"/>
                
                <Canvas x:Name="AnimationCanvas" Grid.Row="1" Background="#1A252F" ClipToBounds="True">
                    <Ellipse x:Name="Ball" Width="50" Height="50" Canvas.Left="100" Canvas.Top="100">
                        <Ellipse.Fill>
                            <SolidColorBrush x:Name="BallBrush" Color="#E74C3C"/>
                        </Ellipse.Fill>
                    </Ellipse>
                </Canvas>
                
                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="10">
                    <Button x:Name="StartStopButton" Content="▶ Старт" Width="100" Click="StartStop_Click"/>
                    <TextBlock x:Name="BounceCountText" Text="Отскоков: 0" Foreground="White" VerticalAlignment="Center" Margin="20,0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
