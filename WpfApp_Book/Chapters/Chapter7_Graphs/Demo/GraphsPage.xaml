<Page x:Class="WpfApp_Book.Chapters.Chapter7_Graphs.Demo.GraphsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Глава 7: Графы" Focusable="True" Loaded="Page_Loaded" KeyDown="Page_KeyDown">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Margin="20,20,20,10">
            <TextBlock Text="Глава 7: Графы и поиск пути" FontSize="28" FontWeight="Bold" Foreground="#2E86AB"/>
            
            <Border Background="#F0F4F8" CornerRadius="8" Padding="15" Margin="0,15,0,0">
                <StackPanel>
                    <TextBlock Text="🔍 BFS — поиск кратчайшего пути" FontSize="16" FontWeight="Bold"/>
                    <Border Background="#2C3E50" CornerRadius="4" Padding="10" Margin="0,10,0,0">
                        <TextBlock FontFamily="Consolas" Foreground="White" FontSize="11">
                            Queue&lt;Point&gt; queue = new();&#x0a;queue.Enqueue(start);&#x0a;while(queue.Count &gt; 0) { ... проверяем соседей ... }
                        </TextBlock>
                    </Border>
                </StackPanel>
            </Border>
        </StackPanel>
        
        <Border Grid.Row="1" Background="#2C3E50" Margin="20,0,20,20" CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" Text="🏃 Лабиринт (стрелки, пробел — путь)" FontSize="16" FontWeight="Bold" Foreground="White" Margin="10"/>
                <Canvas x:Name="MazeCanvas" Grid.Row="1" Background="#1A252F" ClipToBounds="True" SizeChanged="MazeCanvas_SizeChanged"/>
                
                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="10">
                    <Button Content="↻ Новый" Width="80" Click="NewMaze_Click"/>
                    <Button Content="🔍 Путь" Width="80" Margin="10,0" Click="ShowPath_Click"/>
                    <TextBlock x:Name="StatusText" Text="Найди выход! 🏁" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
