<Page x:Class="WpfApp_Book.Chapters.Chapter4_Layout.Demo.LayoutPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Глава 4: Размещение">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
        <StackPanel>
            <TextBlock Text="Глава 4: Размещение и Drag-and-Drop" 
                       FontSize="28" FontWeight="Bold" Margin="0,0,0,20" Foreground="#2E86AB"/>
            
            <Border Background="#F0F4F8" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="📐 Canvas" FontSize="18" FontWeight="Bold" Foreground="#1A5276"/>
                    <Border Background="#2C3E50" CornerRadius="4" Padding="10" Margin="0,10,0,0">
                        <TextBlock FontFamily="Consolas" Foreground="White" FontSize="11">
                            Canvas.SetLeft(element, x);&#x0a;Canvas.SetTop(element, y);&#x0a;double x = Canvas.GetLeft(element);
                        </TextBlock>
                    </Border>
                </StackPanel>
            </Border>

            <Border Background="#FEF9E7" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="🖐️ Drag-and-Drop" FontSize="18" FontWeight="Bold" Foreground="#B7950B"/>
                    <Border Background="#2C3E50" CornerRadius="4" Padding="10" Margin="0,10,0,0">
                        <TextBlock FontFamily="Consolas" Foreground="White" FontSize="11">
                            MouseDown: isDragging = true; element.CaptureMouse();&#x0a;MouseMove: if(isDragging) Canvas.SetLeft(...);&#x0a;MouseUp: isDragging = false; element.ReleaseMouseCapture();
                        </TextBlock>
                    </Border>
                </StackPanel>
            </Border>

            <Border Background="#D5F5E3" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="🎮 Демонстрация" FontSize="18" FontWeight="Bold" Foreground="#27AE60"/>
                    <Canvas x:Name="DemoCanvas" Background="White" Height="200" Margin="0,10,0,0" ClipToBounds="True">
                        <Border x:Name="BlueBox" Canvas.Left="50" Canvas.Top="50" Width="60" Height="60" 
                                Background="#3498DB" CornerRadius="5" Cursor="Hand"
                                MouseDown="Box_MouseDown" MouseMove="Box_MouseMove" MouseUp="Box_MouseUp"/>
                        <Border x:Name="RedBox" Canvas.Left="200" Canvas.Top="80" Width="60" Height="60" 
                                Background="#E74C3C" CornerRadius="5" Cursor="Hand"
                                MouseDown="Box_MouseDown" MouseMove="Box_MouseMove" MouseUp="Box_MouseUp"/>
                    </Canvas>
                    <TextBlock x:Name="DistanceText" Text="Расстояние: —" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <Border Background="#FADBD8" CornerRadius="8" Padding="15" BorderBrush="#E74C3C" BorderThickness="2">
                <StackPanel>
                    <TextBlock Text="📋 Задание: Drag-and-drop с расстоянием" FontSize="18" FontWeight="Bold" Foreground="#C0392B"/>
                    <TextBlock TextWrapping="Wrap" Margin="0,10,0,0" FontSize="13">
                        • 4-5 квадратов на Canvas&#x0a;
                        • Отображать расстояние до центра&#x0a;
                        • При пересечении менять цвет
                    </TextBlock>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
